# Reservation API: notifyCheckout

**Endpoint:** `POST /api/reservation/notifyCheckout`

**Description:** Logs a notification message for each overdue item. The request body should include a `message` field, which will be logged as the content of the notification. No email is sent.

**Request Body:**

```json
{
  "message": "string"
}
```

**Response:**

- `200 OK`: Array of kerb identifiers notified
  - Content-Type: application/json
  - Schema:
    ```json
    ["kerb1", "kerb2", ...]
    ```

**Example Request:**

```json
{
  "message": "Reminder: Please return your overdue items."
}
```

**Example Response:**

```json
["jdoe", "asmith"]
```

**Tags:** Reservation
